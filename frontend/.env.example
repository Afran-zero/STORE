VITE_API_BASE_URL=http://localhost:8000

# Set to "true" to enable Server-Sent Events streaming for the AI chat endpoint
# (POST /api/v1/ai/chat/stream). Requires AI_STREAMING_ENABLED=true on the backend.
VITE_AI_STREAMING=false
