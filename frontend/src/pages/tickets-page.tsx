import { Card } from '@/components/ui/card';

export function TicketsPage(): JSX.Element {
  return <Card><h3 className="text-lg font-bold tracking-tight">Tickets</h3><p className="mt-2 text-sm text-zinc-500">Kanban and ticket detail views.</p></Card>;
}
