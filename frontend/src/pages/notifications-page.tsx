import { Card } from '@/components/ui/card';

export function NotificationsPage(): JSX.Element {
  return <Card><h3 className="text-lg font-bold tracking-tight">Notifications</h3><p className="mt-2 text-sm text-zinc-500">Unread list and mark-all-read actions.</p></Card>;
}
